git clean -xdf   清理文件

git remote prune origin    清理本地分支

git merge  合并分支

git push origin --delete <branch>   删除远程分支

git add -u  暂存已文件

git add -A   暂存所有文件


git tag <tagname>   为当前版本打tag

git tag -a <tagname> commit -m <comment>  为特定版本打tag


git reset  --hard <commit>  强制回滚

git reset --hard HEAD～3     强制回滚3个版本

git push -f

git s 查看上传内容


- git upload repository

git config --global user.name ""

git config --global email.name "15638515778@163.com"

git init

git add .

git commit -m ""

git remote add origin repository_url  

git push -u origin master 

- delete the .git file hidden in the project

git rm --cached -r .idea .gradle XXX

git commit -m "delete XXX"

git push origin master


- git add

git add .误添加了所有文件

git reset HEAD 撤回所有添加

不建议使用git add .

git add -u：将文件的修改、**删除**，添加到暂存区。

git add .：将文件的修改，**新建**，添加到暂存区。

git add -A：将文件的修改，**删除\新建**，添加到暂存区

git add -u [<path>]: 

把<path>中所有tracked文件中被修改过或已删除文件的信息添加到索引库。
它不会处理untracted的文件。

git add -A: [<path>]

表示把<path>中所有tracked文件中被修改过或已删除文件
和**所有untracted文件**信息添加到索引库。

- git branch newbranch

**将新分支合并到主线**

- git checkout master

- git merge newbranch

**2020.5.26更新**

- git pull
    
  
